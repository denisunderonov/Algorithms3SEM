# 🌳 ЧТО ТАКОЕ ДВОИЧНОЕ ДЕРЕВО?

## ОСНОВЫ

**Дерево** - это структура данных, состоящая из узлов, соединённых рёбрами.

**Двоичное дерево** - каждый узел имеет максимум 2 ребёнка (левый и правый).

**Двоичное дерево поиска (BST)** - специальное правило:
- Всё что МЕНЬШЕ родителя → идёт ВЛЕВО
- Всё что БОЛЬШЕ родителя → идёт ВПРАВО


## ВИЗУАЛИЗАЦИЯ

```
         50              ← КОРЕНЬ (root)
        /  \
      30    70           ← Дети корня
     /  \   /  \
   20  40 60  80         ← Листья (нет детей)
```

**Термины:**
- **Корень** - верхний узел (50)
- **Лист** - узел без детей (20, 40, 60, 80)
- **Поддерево** - часть дерева от любого узла
- **Высота** - максимальное количество уровней
- **Уровень** - расстояние от корня


## КАК РАБОТАЕТ ВСТАВКА?

Добавим число 35 в дерево выше:

```
Шаг 1: 35 < 50 → идём влево
         50
        /
      30

Шаг 2: 35 > 30 → идём вправо
      30
        \
        40

Шаг 3: 35 < 40 → идём влево, места нет → вставляем!
        40
       /
      35
```

Результат:
```
         50
        /  \
      30    70
     /  \   /  \
   20  40 60  80
       /
      35
```


## КАК РАБОТАЕТ УДАЛЕНИЕ?

**3 случая:**

### 1. Удаление ЛИСТА (нет детей)
Удаляем 20:
```
До:          После:
    30          30
   /  \           \
  20  40          40
```
Просто убираем узел.


### 2. Удаление узла с ОДНИМ ребёнком
Удаляем 30 (есть только правый 40):
```
До:          После:
    50          50
   /              \
  30              40
    \
    40
```
Заменяем узел его ребёнком.


### 3. Удаление узла с ДВУМЯ детьми (самый сложный)
Удаляем 50 (есть 30 и 70):
```
До:              После:
      50              60     ← минимум из правого
     /  \            /  \
   30    70        30    70
        /  \            /  \
      60   80         NULL 80
```

**Алгоритм:**
1. Найти минимум в правом поддереве (60)
2. Заменить удаляемое значение на 60
3. Удалить 60 из правого поддерева


## ПОИСК В ГЛУБИНУ (DFS - Depth First Search)

**Идея:** идём максимально глубоко, пока можем.

### 1. Симметричный (In-order): ЛЕВЫЙ → КОРЕНЬ → ПРАВЫЙ
```
         50
        /  \
      30    70
     /  \
   20   40

Порядок: 20, 30, 40, 50, 70
Результат: ОТСОРТИРОВАННЫЙ СПИСОК! ✨
```

### 2. Прямой (Pre-order): КОРЕНЬ → ЛЕВЫЙ → ПРАВЫЙ
```
Порядок: 50, 30, 20, 40, 70
Используется: копирование дерева
```

### 3. Обратный (Post-order): ЛЕВЫЙ → ПРАВЫЙ → КОРЕНЬ
```
Порядок: 20, 40, 30, 70, 50
Используется: удаление дерева (сначала дети, потом родитель)
```


## ПОИСК В ШИРИНУ (BFS - Breadth First Search)

**Идея:** обходим уровень за уровнем.

```
         50           Уровень 0
        /  \
      30    70        Уровень 1
     /  \   /  \
   20  40 60  80     Уровень 2

Порядок: 50, 30, 70, 20, 40, 60, 80
```

**Используется:** поиск кратчайшего пути, обход по слоям.


## РЕКУРСИЯ vs ИТЕРАЦИЯ

### РЕКУРСИЯ
**Плюсы:**
- Код короче и понятнее
- Естественно для деревьев

**Минусы:**
- Может вызвать переполнение стека при большой глубине

```python
def dfs_inorder_recursive(node):
    if node is not None:
        dfs_inorder_recursive(node['left'])   # Рекурсия влево
        print(node['value'])
        dfs_inorder_recursive(node['right'])  # Рекурсия вправо
```


### ИТЕРАЦИЯ
**Плюсы:**
- Нет риска переполнения стека
- Больше контроля

**Минусы:**
- Код сложнее
- Нужно вручную управлять стеком/очередью

```python
def dfs_inorder_iterative(root):
    stack = []
    current = root
    
    while current or stack:
        while current:
            stack.append(current)
            current = current['left']
        
        current = stack.pop()
        print(current['value'])
        current = current['right']
```


## СТЕК vs ОЧЕРЕДЬ

**DFS использует СТЕК** (LIFO - Last In First Out)
```
Стек: [5, 3, 1]
pop() → 1  (последний вошёл, первый вышел)
```

**BFS использует ОЧЕРЕДЬ** (FIFO - First In First Out)
```
Очередь: [5, 3, 1]
pop(0) → 5  (первый вошёл, первый вышел)
```


## СЛОЖНОСТЬ ОПЕРАЦИЙ

| Операция | Среднее | Худшее     |
|----------|---------|------------|
| Поиск    | O(log n)| O(n)       |
| Вставка  | O(log n)| O(n)       |
| Удаление | O(log n)| O(n)       |

**O(log n)** - если дерево сбалансировано
**O(n)** - если дерево превратилось в список:
```
1
 \
  2
   \
    3
     \
      4  ← это уже не дерево, а список!
```


## ПРИМЕНЕНИЕ В РЕАЛЬНОЙ ЖИЗНИ

1. **Файловые системы** - папки и файлы
2. **DOM дерево** - HTML структура веб-страниц
3. **Базы данных** - индексы для быстрого поиска
4. **Компиляторы** - дерево синтаксического разбора
5. **Игры** - дерево решений AI


## ЗАПУСК ПРОГРАММЫ

```bash
python binary_tree.py
```

Вы увидите:
- Вставку элементов с визуализацией
- Все виды обходов (DFS и BFS)
- Удаление элементов разных типов
- Структуру дерева после каждого изменения


## ИНТЕРАКТИВНАЯ ВЕРСИЯ

Хотите попробовать сами? Используйте:

```python
tree = None
tree = insert(tree, 50)
tree = insert(tree, 30)
tree = insert(tree, 70)
display_tree_compact(tree)

print(dfs_inorder_recursive(tree))  # [30, 50, 70]

tree = delete(tree, 30)
display_tree_compact(tree)
```
