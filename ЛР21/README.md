# ЛР21: Анализ сложности алгоритмов

## 📋 Описание

Эта лабораторная работа тестирует **сложность алгоритмов** на реальных данных размером **100, 1000 и 10000 элементов**.

## 🎯 Проверяемые алгоритмы

### 1. **Бинарное дерево поиска (BST)**
- **Операции:** вставка, поиск, удаление
- **Теоретическая сложность:**
  - Средний случай: O(log n)
  - Худший случай: O(n) (вырожденное дерево)

### 2. **Хеш-таблица с цепочками**
- **Операции:** вставка, поиск, удаление
- **Обработка коллизий:** метод цепочек
- **Теоретическая сложность:**
  - Средний случай: O(1)
  - Худший случай: O(n) (все в одной цепочке)

### 3. **Пирамидальная сортировка (Heap Sort)**
- **Операция:** сортировка массива
- **Теоретическая сложность:** O(n log n) всегда

### 4. **Обходы графа (BFS и DFS)**
- **Реализации:**
  - BFS (обход в ширину)
  - DFS рекурсивный (обход в глубину)
  - DFS итеративный (обход в глубину через стек)
- **Теоретическая сложность:**
  - Список смежности: O(n + m)
  - **Матрица смежности: O(n²)** ← используется в этой ЛР

## 🚀 Запуск

```bash
cd ЛР21
python3 complexity_analysis.py
```

## 📊 Что измеряется

Для каждого размера (100, 1000, 10000) программа:

1. **BST:**
   - Вставляет все элементы
   - Ищет случайные 100 элементов
   - Удаляет случайные 100 элементов

2. **Хеш-таблица:**
   - Вставляет все элементы
   - Ищет случайные 100 элементов
   - Удаляет случайные 100 элементов
   - Показывает статистику коллизий

3. **Пирамидальная сортировка:**
   - Сортирует случайный массив

4. **Граф (только для 100 и 1000):**
   - Создаёт связный граф
   - Выполняет BFS, DFS (рекурсивный и итеративный)
   - **Примечание:** для 10000 вершин пропускается (O(n²) слишком медленно)

## 📈 Ожидаемые результаты

### Рост времени при увеличении размера в 10 раз:

| Алгоритм | Сложность | Рост времени (примерно) |
|----------|-----------|------------------------|
| **BST вставка** | O(n log n) | ×13 раз (10 × log 10) |
| **Хеш-таблица** | O(n) | ×10 раз |
| **Heap Sort** | O(n log n) | ×13 раз |
| **BFS/DFS (матрица)** | O(n²) | ×100 раз |

### Пример вывода:

```
--- Бинарное дерево поиска (BST), размер: 100 ---
  Вставка 100 элементов: 0.000234 сек
  Поиск 100 элементов: 0.000089 сек
  Удаление 100 элементов: 0.000112 сек

--- Хеш-таблица, размер: 100 ---
  Вставка 100 элементов: 0.000156 сек
  Поиск 100 элементов: 0.000034 сек
  Удаление 100 элементов: 0.000042 сек
  Коллизий: 8/10 (80.0%)
  Макс. длина цепочки: 15

--- Пирамидальная сортировка, размер: 100 ---
  Сортировка 100 элементов: 0.000198 сек

--- Обходы графа, размер: 100 вершин ---
  BFS: 0.012345 сек
  DFS (рекурсивный): 0.011234 сек
  DFS (итеративный): 0.010987 сек
```

## 🔍 Анализ результатов

### 1. **BST vs Хеш-таблица**
- **Хеш-таблица быстрее** для поиска и вставки (O(1) vs O(log n))
- **BST** поддерживает упорядоченность данных

### 2. **Коллизии в хеш-таблице**
- Размер таблицы = 10% от количества элементов
- Демонстрирует влияние коллизий на производительность
- Много коллизий → длинные цепочки → медленный поиск

### 3. **Heap Sort**
- Стабильная сложность O(n log n)
- Не зависит от исходного порядка данных

### 4. **BFS vs DFS**
- **Итеративный DFS обычно быстрее** рекурсивного (нет overhead вызовов функций)
- **BFS может быть медленнее** из-за операций с очередью
- **Матрица смежности O(n²)** — для больших графов лучше использовать список смежности

### 5. **Почему граф для 10000 пропускается?**
- Матрица смежности для 10000 вершин = 10000² = 100 млн элементов
- Каждый обход проверяет O(n²) рёбер → очень медленно
- Решение: использовать список смежности (O(n + m))

## 💡 Выводы

1. **Выбор структуры данных важен:**
   - Хеш-таблица — для быстрого доступа без порядка
   - BST — для упорядоченных данных
   - Heap — для приоритетных очередей и сортировки

2. **Представление графа критично:**
   - Матрица смежности: O(n²) память, медленные обходы
   - Список смежности: O(n + m) память, быстрые обходы

3. **Коллизии в хеш-таблицах:**
   - Размер таблицы должен быть достаточно большим
   - Load factor (коэффициент заполнения) обычно ≤ 0.75

4. **Рекурсия vs Итерация:**
   - Итеративные версии обычно быстрее
   - Рекурсия может привести к переполнению стека

## 📚 Дополнительные эксперименты

Можете модифицировать код для:
- Тестирования на других размерах (50, 500, 5000)
- Сравнения сбалансированных и вырожденных BST
- Изменения размера хеш-таблицы и анализа коллизий
- Использования списка смежности для графов (добавьте свою реализацию)
- Сравнения heap sort с quick sort и merge sort

## 🎓 Теоретическая справка

### Сложности основных операций:

| Структура | Поиск | Вставка | Удаление | Память |
|-----------|-------|---------|----------|--------|
| **BST (средний)** | O(log n) | O(log n) | O(log n) | O(n) |
| **BST (худший)** | O(n) | O(n) | O(n) | O(n) |
| **Хеш-таблица (средний)** | O(1) | O(1) | O(1) | O(n) |
| **Хеш-таблица (худший)** | O(n) | O(n) | O(n) | O(n) |
| **Heap (куча)** | O(n) | O(log n) | O(log n) | O(n) |

### Сортировки:

| Алгоритм | Лучший | Средний | Худший | Память |
|----------|--------|---------|--------|--------|
| **Heap Sort** | O(n log n) | O(n log n) | O(n log n) | O(1) |
| **Quick Sort** | O(n log n) | O(n log n) | O(n²) | O(log n) |
| **Merge Sort** | O(n log n) | O(n log n) | O(n log n) | O(n) |

---

**Удачи в анализе алгоритмов!** 🚀
